User-agent: *
Allow: /

# ochem/notes/ holds the prose half of each lesson as bare HTML fragments —
# no <title>, no stylesheet, no navigation — because the lesson page and the
# study assistant fetch them and inject them into a page that has all three.
# Indexed on their own they would rank as pages that are a wall of unstyled
# text with no way out. The lesson pages that use them are in the sitemap.
Disallow: /ochem/notes/

# The full question bank is the editable source the two runtime files are
# generated from; nothing on the site fetches it, so there is nothing to index.
Disallow: /nremt/assets/questions.json

Sitemap: https://levlprep.com/sitemap.xml
